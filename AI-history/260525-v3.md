# ExpertEye360 — AI 作業履歴（Phase 1 shared 抽出・テスト）

**日付**: 2026-05-25

**前提履歴**: [260525-v2.md](./260525-v2.md)（Phase 0 TDD・Red → Green・文書整合）

---

## 1. 本セッションのテーマ

Phase 1 として、受講者コアと管理者カード入力の一部ロジックを `shared/src` に抽出し、Vitest で固定した。

---

## 2. 実施内容

### `cardSlots`

- `shared/src/cardSlots.ts` を追加
- `cardsToSlots()` / `slotsToCards()` を `admin-web/src/components/CardSlotsField.tsx` から shared へ移動
- `shared/src/cardSlots.test.ts` を追加

### `selection`

- `shared/src/selection.ts` を追加
- `selectSingle()` を `ParticipantPage.tsx` から shared へ移動
- `shared/src/selection.test.ts` を追加

### `validateStep`

- `shared/src/validateStep.ts` を追加
- `validateParticipantStep()` として step バリデーションを shared 化
- `shared/src/validateStep.test.ts` を追加

### `submission`

- `shared/src/submission.ts` を追加
- `buildSubmission()` として送信ペイロード組み立てを shared 化
- `shared/src/submission.test.ts` を追加

---

## 3. Refactor

- `ParticipantPage.tsx` は `selectSingle()` / `validateParticipantStep()` / `buildSubmission()` を呼ぶ形に変更
- `CardSlotsField.tsx` は `cardsToSlots()` / `slotsToCards()` を shared から再 export する形に変更
- `AdminPage.tsx` の `EMPTY_VETERAN_TEMPLATE` から `as const` を外し、型チェックの readonly 配列エラーを解消

---

## 4. テスト結果

```bash
npm test
```

```text
Test Files  8 passed
Tests       37 passed
```

```bash
npm run typecheck --prefix participant-web
npm run typecheck --prefix admin-web
```

```text
両方成功
```

---

## 5. 更新したドキュメント

- [README.md](../README.md) — 自動テスト本数を 37 本 Green に更新
- [docs/DOC-ALIGNMENT.md](../docs/DOC-ALIGNMENT.md) — Phase 1 shared 抽出 Green とテスト本数を更新
- [docs/TEST-DESIGN.md](../docs/TEST-DESIGN.md) — 現在マイルストーンを Phase 2 に更新、Phase 1 チェックリストを完了化
- [docs/TDD-FEATURE-INVENTORY.md](../docs/TDD-FEATURE-INVENTORY.md) — Phase 1 抽出済みモジュールの状態を更新

---

## 6. 次セッション候補

Phase 2 に進む場合:

1. `storage.test.ts`（localStorage 往復・破損 JSON・イベント発火）
2. `seed.test.ts`
3. 管理者保存の正規化確認（既存 `sceneQuestions.test.ts` の継続）

---

## 7. 未実施

- git commit / push
- Sheet API / GAS
- PDF
- OJT
- Playwright E2E
